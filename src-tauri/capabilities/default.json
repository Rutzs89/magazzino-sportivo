{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permessi della finestra principale. Niente updater: il controllo e l'installazione li fa la parte Rust, la pagina non chiama mai quel plugin. dialog:allow-confirm serve alle domande di conferma: senza, il componente sostituisce la conferma con una che viene rifiutata, e le domande non compaiono.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-confirm"
  ]
}
